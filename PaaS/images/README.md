### Custom image installation ###
Go inside each folder and run build.sh or install.sh

### Images to be pulled from online repository ###
- Mosquitto (MQTT server: https://hub.docker.com/_/eclipse-mosquitto)
	sudo docker pull eclipse-mosquitto
- Redis (Key-value storage ans cache: https://hub.docker.com/_/redis)
	sudo docker pull redis
